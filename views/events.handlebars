<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <div class="navbar-brand">
        <p>ALCOVENT</p>
      </div>
    </div>
  </div>
</nav>

<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <div class="dropdown">
            <div class="form-group">
        <label for="exampleSelect1">Choose Your Event</label>
        <select class="form-control" id="exampleSelect1">
           <option>Choose Your Event </option>
           {{#each eventObj}}
            <option value={{id}}>{{type}}</option>
           {{/each}}
        </select>
      </div>
      </div>
    </div>
  </div>
</div>